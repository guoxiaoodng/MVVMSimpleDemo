<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_ready"
    app:startDestination="@id/main">
    <!--<fragment
        android:id="@+id/splash"
        android:name="com.data.hemo.mobile.ui.fragment.ready.SplashFragment"
        android:label="闪屏页"
        tools:layout="@layout/fragment_splash">

        <action
            android:id="@+id/action_splash_to_login"
            app:destination="@id/login"
            app:enterAnim="@anim/window_right_in"
            app:exitAnim="@anim/window_left_out"
            app:popEnterAnim="@anim/window_left_in"
            app:popExitAnim="@anim/window_right_out" />
        <action
            android:id="@+id/action_splash_to_main"
            app:destination="@id/main"
            app:enterAnim="@anim/fade_in"
            app:exitAnim="@anim/fade_out"
            app:popEnterAnim="@anim/window_left_in"
            app:popExitAnim="@anim/window_right_out" />
        <action
            android:id="@+id/action_splash_to_guide"
            app:destination="@id/guide"
            app:enterAnim="@anim/window_right_in"
            app:exitAnim="@anim/window_left_out"
            app:popEnterAnim="@anim/window_left_in"
            app:popExitAnim="@anim/window_right_out" />
    </fragment>-->

    <fragment
        android:id="@+id/guide"
        android:name="com.data.hemo.mobile.ui.fragment.ready.GuideFragment"
        android:label="引导页1"
        tools:layout="@layout/fragment_guide">
        <action
            android:id="@+id/action_guide_to_login"
            app:destination="@id/login"
            app:enterAnim="@anim/fade_in"
            app:exitAnim="@anim/fade_out"
            app:popEnterAnim="@anim/window_left_in"
            app:popExitAnim="@anim/window_right_out" />
    </fragment>
    <activity
        android:id="@+id/login"
        android:name="com.data.hemo.mobile.ui.activity.LoginActivity"
        android:label="登录页"
        tools:layout="@layout/fragment_login" />
    <activity
        android:id="@+id/main"
        android:name="com.data.hemo.mobile.ui.activity.MainActivity"
        android:label="主页"
        tools:layout="@layout/activity_main" />
</navigation>